# V2 / Tea as a system

Warm paper `#f3f1ea`, dark ink `#172e28`, hairline rules, generous empty space. IBM Plex Sans for language and IBM Plex Mono for every numeric surface. No rounded content cards, illustrations, people or venue photographs. Seven numbered sections; desktop index rail; mobile progress line. Only the economic calculation source is retained from v1.

The thesis is typographic. Product photography begins in section 02 so image decoding cannot block the initial headline. The production-quality turntable is blocked by generation credits; no approximate substitute is presented as a finished sequence.

## Localization and refinement / 14 September

Russian uses natural editorial headlines, neutral contact language, Cyrillic financial terminology, decimal commas and grouped figures. UAH and USD remain the actual planning currencies. The inline headline font now includes all three languages.

The language selector is a compact, keyboard-operable disclosure. Each locale has server-rendered document language, alternate URLs and a localized one-pager. Section rules reveal independently of text; glyphs never scale horizontally. Press feedback uses native animation, with reduced-motion and pointer-cancellation handling. Navigation tolerates disabled storage and restores the page after back navigation. Small mobile disclosures and several touch targets are enlarged.

The refinement process is measure → identify → correct → verify, covering copy, numeric boundaries, routes, document semantics, keyboard use, visible layouts, motion, PDF output and performance. It does not certify unavailable media or physical hardware.
